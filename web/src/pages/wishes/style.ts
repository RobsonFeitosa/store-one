import { Heading, styled } from '@lemonade-technologies-hub-ui/react'

export const WishesContainer = styled('div', {})

export const WishesContent = styled('div', {})

export const WishHeader = styled('div', {
  marginBottom: '$4',
  [`${Heading}`]: {},
})

export const WisheWrapper = styled('div', {})
