import { styled } from '@lemonade-technologies-hub-ui/react'

export const WelcomeContainer = styled('div', {
  marginBottom: 20,

  'span, strong': {
    fontSize: '$lg',
  },

  p: {
    display: 'flex',
    gap: '$2',
  },
})
