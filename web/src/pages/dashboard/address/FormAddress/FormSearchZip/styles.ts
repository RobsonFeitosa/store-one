import { Button, styled } from '@lemonade-technologies-hub-ui/react'

export const FormSearchZipContainer = styled('div', {
  marginTop: 20,
})

export const BtnButton = styled(Button, {})

export const Form = styled('div', {
  display: 'flex',
  gap: '$6',

  '> div': {
    width: '100%',
  },
})
